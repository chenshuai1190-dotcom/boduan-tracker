# Security Hardening Runbook

This project started as a personal hand-built app, so the first priority is to make the current baseline safe to maintain.

## Immediate Manual Actions

1. Rotate the EODHD API token.
   - The old token appeared in repository documentation.
   - Update `EODHD_API_KEY` in Vercel after rotation.
   - Do not add the replacement token to `CONTEXT.md`, screenshots, chat logs, or frontend `VITE_` variables.

2. Verify Vercel environment variables.
   - Required: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `EODHD_API_KEY`.
   - Required for server-side admin flows: `SUPABASE_SERVICE_ROLE_KEY`. Keep it server-only; never create a `VITE_` variant.
   - Required for automated close snapshots: `CRON_SECRET`. This protects `/api/close-snapshot-schedule*`, `/api/pnl-report-daily-snapshot`, and `/api/community-competition-daily-snapshot` and must only exist in Vercel server-side environment variables.
   - Recommended: `QUOTE_API_AUTH_REQUIRED=true`.
   - Recommended: `QUOTE_ALLOWED_ORIGINS=https://boduan-tracker.vercel.app`.
   - Do not add any frontend `VITE_` EODHD token or browser WebSocket toggle.

3. Apply Supabase RLS.
   - Run `supabase/rls.sql` in the Supabase SQL editor.
   - Confirm each user-owned table has RLS enabled and policies scoped to `auth.uid() = user_id`.
   - Run `npm run verify:rls:rest` to confirm anonymous REST clients cannot see user-owned rows and cannot execute any listed service-only RPC. After database source commit `0bc0ef2cf423e9f4ac91daafc9cf8c68ba3c7d16` and its exact production migration, production is verified at 22 tables plus both service-only competition RPCs (`join_community_competition_member` and `rebaseline_community_competition_member`), including anonymous `401` for `snapshot_publication_markers`.
   - Production `swing_waves` status (2026-07-11): preflight confirmed the table was absent, `supabase/swing_waves.sql` was applied to project `ykgotnmtqcqdzqtrlayq`, and the schema/grant/RLS metadata audit passed 13/13 checks. Keep this preflight/apply/audit sequence for future schema changes.
   - `swing_waves` must remain independent from `trades`, `stock_trades`, `cost_basis_trades`, and P&L snapshots. Do not include legacy-wave deletion in the table/RLS migration.
   - `community_profiles` status (2026-07-12): `supabase/community_profiles.sql` has been applied to production. It stores only nickname/avatar metadata; authenticated clients may read/insert/update only their own row, and active-member public identity is mediated by the competition API. No delete grant is given, and it remains outside trades, assets, returns, quote relay, and Storage uploads.
   - `v10.7.9.312` expands only the `community_profiles_avatar_key_check` allowlist from 6 to 18 preset keys. Apply `supabase/community_avatar_options_v312.sql` before the frontend deploy, confirm all existing rows still satisfy the constraint, and keep the same owner-only RLS policies and table grants.
   - `v10.7.9.315` requires `supabase/registration_community_profile_v315.sql` before application deployment. The production migration was applied and read back on 2026-07-12: `service_role` SELECT/INSERT is true, anonymous INSERT is false, authenticated INSERT and RLS remain true, and the authenticated owner SELECT/INSERT/UPDATE policies are unchanged.
   - `community_competition_members` and `community_competition_snapshots` are a separate competition boundary. Their prior production SQL was applied on 2026-07-12 and the 20-table anonymous REST gate passed. Authenticated clients may only read their own membership; they cannot write membership or read/write competition snapshots. Snapshot `service_role` privileges remain limited to select/insert so a locked row cannot be overwritten or deleted.
   - Production revision/CAS status (2026-07-14): database source commit `0f52700761beab0d4488e067ca9e968aea9a9bc1` passed Actions `29320513471`, and the exact committed `supabase/community_competition_rebaseline_20260714.sql` was applied to project `ykgotnmtqcqdzqtrlayq`. It installs server-authored trade timestamps, opaque `stock_trade_ledger_revisions`, service-only join/rebaseline CAS functions, and membership/snapshot insert guards. The 5-second fail-fast writer lock covered only existing-ledger seed plus the two stock-trade triggers, so no mutation could fall between seed and trigger installation and no unbounded writer queue was allowed. Metadata readback confirmed owners, SECURITY DEFINER/invoker flags, fixed `search_path`, four triggers, grants, FORCE RLS, lock order, and old overload removal. `npm run verify:rls:rest` passed 21 table probes plus both anonymous RPC denials. A real two-session revision-row-then-member-row smoke also passed with both transactions rolled back and no identity or business row returned. Runtime `8f23a471be3cd63b657bf1f7a807c438881a23ea` passed Actions `29321470173` and Vercel target `CkNECvKe9N3WGSLokcaWFdYkUxvg`; unauthenticated quote, market-movers, earnings, P&L benchmark, competition, and both GET-only snapshot routes reject with `401`.
   - `v10.7.9.346` database-first stage is complete: `supabase/snapshot_publication_markers_20260716.sql` was committed as `0bc0ef2cf423e9f4ac91daafc9cf8c68ba3c7d16`, its exact SHA-256 was verified before production execution, and metadata/grant readback plus the 22-table gate passed. The table stores only `channel`, `snapshot_date`, opaque `version`, and `completed_at`; it contains no user identifier or financial data. FORCE RLS is enabled, PUBLIC/anon/authenticated have no table privileges, and only `service_role` may select/insert/update. Runtime code is deployed in `v10.7.9.347`.
   - `v10.7.9.348` completed the one-time upgrade repair through `supabase/snapshot_publication_marker_bootstrap_20260716.sql`. Source commit `3c85f64c0dcb26afd4b6b776f1a4039a7b0fb961` and the exact SQL SHA-256 `b53314d864dd568d5525814de681be9e3d758edf2dc1da8654f85ed5080de806` were verified before production execution. It ran only while the competition marker was empty, considered only dates before 2026-07-16, locked the profile/member/snapshot/marker boundary, and selected the newest exact complete locked batch with completed profiles, valid eligibility/ranking baselines, strict 64-hex hashes, non-negative revisions, and no missing or unexpected rows. Production postflight confirmed marker date `2026-07-13` / version `verified_bootstrap_20260716`, with 8 expected and 8 complete members, no missing rows, and one later-start member. The 23 locked snapshots, their `8 / 7 / 8` distribution, and digest `4e144e79415dd4f423bcfd76b8fe500b` were unchanged. It inserted only the service-only marker and did not create, modify, delete, or backfill any snapshot, return, ranking, trade, or holding. The 2026-07-14/15 gaps stay with the formal post-17:00 ET scheduler.

4. Validate production behavior.
   - Login works.
   - Add/delete a trade only affects the current user.
   - `/api/quote` returns `401` without a Supabase access token.
   - `/api/pnl-benchmark?symbol=QQQ&from=2026-01-01&to=2026-07-14` returns `401` without a Supabase access token.
   - `/api/community-competition` and `/api/community-competition?operation=snapshot-status` return `401` without a Supabase access token; `/api/close-snapshot-schedule`, `/api/community-competition-daily-snapshot`, and `/api/pnl-report-daily-snapshot` return `401` without `CRON_SECRET`.
   - `/api/btc-realtime`, `/api/indices-realtime`, and `/api/stocks-realtime` reject unauthenticated WebSocket upgrades.
   - `/api/quote` returns data when called from the logged-in app.

5. Keep invite-only registration enforced.
   - Run `supabase/invite_codes.sql` or the full `supabase/rls.sql` in the Supabase SQL editor before using invite registration.
   - Set `SUPABASE_SERVICE_ROLE_KEY` only in Vercel server-side environment variables; never expose it as a `VITE_` variable or commit it.
   - Keep the frontend registration path on `/api/register`, and disable direct public signups in Supabase Auth if hard invite-only enforcement is required.
   - Registration must validate nickname and preset avatar both in the browser and again in `/api/register`. Create the completed profile before marking the invite used; if either profile creation or invite consumption fails, delete the newly created Auth user. Do not put the community profile in user-editable Auth metadata as a second source of truth.
   - Completing a profile during registration must never create `community_competition_members`; competition enrollment remains a separate voluntary authenticated action.
   - Only `chenshuai1190@gmail.com` should see the invite-code management panel in Settings.

6. Keep automated P&L report snapshots server-only.
   - Vercel Cron calls the three `/api/close-snapshot-schedule*` paths only through the server-side `CRON_SECRET` boundary. They rewrite into the existing P&L serverless function's scheduler branch, resolve one target date in `America/New_York`, and start the independent P&L and competition runners in the same invocation. Before 17:00 ET a no-date weekday request must return a sanitized deferred result without provider or database work.
   - The unified primary, retry, and late-retry paths use UTC `21/22/23` Hobby windows. The 21 UTC winter-time call is intentionally early and must safely defer; later windows provide redundancy. Rewrites must not create new public functions or exceed the Hobby 12-function limit. Per-symbol provider retries remain bounded at three attempts; incomplete work or a missing target-date SPY calendar row must return 503 instead of 200 or silently choosing an older day.
   - A valid explicit `date=YYYY-MM-DD` is a protected manual repair path and may bypass the 17:00 ET gate, but it must first confirm an exact target-date SPY close before any Supabase business-table read or write. A weekend/holiday, stale provider payload, or unavailable exact close must fail closed with zero business-database access. An invalid explicit date must return 400 without provider/database calls. Never turn the explicit path into an unauthenticated backfill endpoint.
   - A no-date scheduled run validates the computed target with SPY, then uses SPY sessions from the latest 31-calendar-day window to catch existing users up after their latest portfolio completion marker. A user with no marker is scheduled only for the target date. For each user/date, delete the portfolio marker before replacing symbol rows and recreate it last. The portfolio/symbol pair is not one database transaction, but this ordering makes partial writes detectable and repairable on the next run.
   - The endpoint must require `Authorization: Bearer <CRON_SECRET>` and must not accept normal frontend Supabase user tokens as a substitute.
   - The endpoint uses `SUPABASE_SERVICE_ROLE_KEY` only on the server to read all users' `stock_trades` and write `pnl_report_snapshots` / `pnl_report_symbol_snapshots`.
   - The route response must stay sanitized: never return `CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `EODHD_API_KEY`, or raw per-user auth material.

7. Keep community competition snapshots independent and server-only.
   - `/api/community-competition` must always validate a Supabase bearer token and must not inherit the optional quote-auth bypass.
   - `/api/community-competition-daily-snapshot` rewrites into the cron-only branch of `api/community-competition.js` to stay within the Hobby 12-function limit. That branch checks `CRON_SECRET` before any work and never falls through to the user Bearer route. It reads the formal USD ledger without modifying it and writes only percentage-only competition snapshots/membership start metadata. `stock_trade_ledger_revisions` is the authoritative opaque mutation state: authenticated clients cannot read or forge it, while database triggers overwrite trade `created_at`/`updated_at` with server time and advance `revision`/`last_mutated_at` on every INSERT, UPDATE, or DELETE. Joining reads that revision before hashing the ledger, then a service-only CAS function locks the revision row followed by the member row and freezes the matching eligible-date hash/revision. The first snapshot must verify the frozen hash and revision rather than trusting client-supplied timestamps. The competition-only cash-flow model must include same-day buy/sell flows, adjusted-close valuation, consecutive trading-date snapshots, empty-portfolio carry, immutable ledger hashes, and per-symbol provider-failure isolation.
   - Automatic competition work now starts from the same protected UTC `21/22/23` scheduler invocation as personal P&L, but its tables, model, error result, D1/D2 state, and manual repair endpoint remain independent. Scheduled runs read the complete SPY session sequence from the earliest pending anchor through the target and process each member from `eligible_after_snapshot_date` or the latest locked row in ascending order, bounded to 5 trading dates or 250 member-days per invocation. Remaining work resumes on a later scheduled run. `missing_close`, `snapshot_gap`, and `trade_between_snapshots` are recoverable incomplete states and return 503; oversell, late records, malformed formal rows, and mismatches for an already-started member remain authoritative rejections. The internal competition accepts positive execution prices from the formal ledger without a provider raw high/low range gate.
   - A competition publication marker may be written only after the whole target-date batch is complete. A partial member row, batch-limited result, failure, or marker-write error must not publish a new leaderboard date. Database trigger time is authoritative for `completed_at`, and the server must preserve its microseconds so same-day repair publications remain ordered. The marker is a durable service-only database boundary, not a Supabase Realtime publication.
   - The one-time v348 marker bootstrap follows the same completeness rule and is not a snapshot repair path. It must fail closed when no exact complete historical batch exists, must lose safely to an already-written scheduler marker, and must leave all competition snapshot and formal-ledger rows byte-for-byte untouched.
   - Authenticated browsers must not read `snapshot_publication_markers` directly. `/api/community-competition?operation=snapshot-status` may return only `channel`, `snapshotDate`, opaque `version`, and `completedAt`. User-scoped cache v4 may use that lightweight status to decide whether to refetch and invalidates the former absolute-return ranking cache while preserving each member's personal period start; it must not cache tokens, email, trades, amounts, holdings, or cross-user data. Status timing and latest observed publication stay in separate user-scoped metadata so they cannot overwrite leaderboard entries. Day/week/month/year share one visible stale-status check per minute, but only the active stale period reads a full leaderboard.
   - Full leaderboard commits and explicit identity/join cache clears must share one browser Web Lock. Publication date, database completion time, and opaque version are monotonic across tabs and periods; an old ready or waiting response cannot roll back a newer marker. A strictly newer authoritative waiting response may replace a formerly ready row when the server's ledger safety rules remove that member. Failed full reads preserve the prior complete cache and use a bounded cooldown instead of a retry loop.
   - A valid explicit snapshot date bypasses only the time gate; before any competition-table read or write it must confirm an exact target-date SPY close. Missing/stale SPY data, weekends, and holidays fail closed with zero competition business-database access. The explicit path must never invoke competition rebaseline. Invalid explicit dates return 400 without provider/database work.
   - Before the first snapshot, only a revision delta fully explained by database-timestamped, unedited pure INSERT rows whose trade date is the current target date and whose authoritative creation time is no later than the target close may follow the normal snapshot path. Any historical edit, deletion, target-row update, mixed or unprovable mutation sequence, migration-era delta-zero ambiguity, or post-close mutation cannot use that exception. It must defer when the target close is not yet authoritative and otherwise enter the D1 forward-only path.
   - Forward-only rebaseline is permitted only during scheduled catch-up for an active member with zero official snapshots across the full table and both ranking fields null. On D1 the worker validates the entire formal ledger without filtering malformed rows and requires a non-empty USD currency, valid side/symbol/date, positive price and shares, and no oversell. It must fetch an exact positive D1 EOD row for SPY and every relevant stock; a missing symbol row, provider failure, or substitution of an older close blocks the RPC. Every trade dated D1 must use authoritative database time proving the same New York date and `created_at <= 16:00 America/New_York`. Its positive formal-ledger execution price is accepted without a provider raw high/low gate. Only then may the RPC move the eligible date/hash/revision forward to D1. D1 writes no snapshot, sets no ranking, and computes no competition return.
   - The first result may be written only on D2, the next real U.S. close after D1, if the ledger is unchanged and the normal snapshot path has all required exact positive EOD closes. Another ledger edit causes another forward move and another waiting day. Any existing snapshot or either ranking field permanently disables rebaseline.
   - `rebaseline_community_competition_member` is executable only by `service_role`, compares the expected old date/hash/revision and current authoritative revision, and rejects same-day or backward movement. Join, rebaseline, and snapshot insertion use one lock order: authoritative revision row first, member row second. The snapshot guard rechecks the current revision under those locks and rejects inactive members, stale revisions, and `snapshot_date <= eligible_after_snapshot_date`. This serializes stock-ledger mutations and overlapping Cron workers without a lock-order inversion and prevents a stale D1 snapshot insert. PUBLIC, anon, and authenticated execution must stay revoked.
   - A joined member with no trades must not receive a fabricated empty snapshot or block another member. When the first later trade appears, scheduled search may accelerate directly to that trade date. Any formal trade dated after the frozen eligible baseline but before the first real SPY close must be authoritatively rejected instead of treated as a pre-existing position. If a locked first snapshot exists but ranking metadata did not persist, recovery must recompute and match both the earliest and latest locked ledger hashes before restoring the earliest locked start date. Locked snapshots remain insert-only and are never repaired by overwrite.
   - Leaderboard responses may return only the ticker codes held at the leaderboard close date, and only after the server-computed official ledger hash matches that same-date locked competition snapshot. A missing or mismatched hash must return an unavailable state. Never return user IDs, email, shares, cost, amounts, allocation, positions, or trade rows. Day ranking uses the target close. For week/month/year, each member keeps the later of the calendar boundary and their fixed personal ranking start; QQQ is calculated from that same personal start and rank is based on `member return - personal-period QQQ return`. A newcomer enters all boards after the first valid close without resetting any existing member's date or accumulated return. Missing or incomplete personal-period QQQ EOD comparison data must make the full leaderboard read fail with a sanitized retryable unavailable response; the client keeps its previous complete cache and must not cache a ready row with null/`--`, mock, realtime, or old-close benchmark values. QQQ must be labeled as the ETF benchmark, not as the NDX index.
   - Historical recovery verification for the previous runtime (2026-07-14): commit `9e1c840e0b336a0352b79f691b7ce3a3b252ff98` passed GitHub Actions run `29313005445` and Vercel deployment `DSGn5mQnzs2o1x6ohQWD6DGrMy2Y`. For snapshot date `2026-07-13`, personal P&L contained 12 portfolio rows for 12 users and 54 symbol rows across 18 symbols; competition contained 8 locked rows for 8 users, with 9 active, 9 eligible, 8 initialized, and 0 invalid snapshot rows. One not-yet-ranked member remained rejected as `eligible_ledger_hash_mismatch`. The prior anonymous REST gate was historically 20/20. This is historical baseline evidence only; the new SQL migration, 21-table + 2-RPC probe, database concurrency smoke and ET-gated runtime deployment are complete, while real-close D1/D2 results remain pending.

8. Keep stock return comparison authenticated, read-only, and fail closed.
   - `/api/pnl-benchmark` must validate the normal Supabase bearer token, keep `EODHD_API_KEY` server-only, use `private, no-store`, and return sanitized errors.
   - The stock detail comparison may read only the signed-in user's formal `stock_trades` and owner-scoped `pnl_report_symbol_snapshots`; it must never write or repair the ledger, snapshots, costs, or holdings.
   - The selected stock and QQQ must both use provider `rawClose` ordinary closes through the same authenticated server boundary. Personal snapshots contribute only the date and held-share integrity check; snapshot close/market-value fields must never substitute for either provider price. The current position-cycle start and selected range determine the requested baseline; the actual baseline is the first date on or after it with an existing personal stock snapshot plus ordinary closes for both symbols, with both sides initialized from the same capital.
   - Later buys mirror the actual executed dollar value into QQQ, and sells reduce QQQ by the same pre-sale holding ratio. Trade/snapshot disagreement, missing required ordinary closes for either symbol, missing exact trade-date QQQ closes, missing final closes, or invalid data must return an unavailable state. Production must never substitute realtime prices, adjusted-return estimates, synthetic rows, or mock returns.
   - `pnl_report_symbol_snapshots` remains owner-scoped but user-writable under the existing personal-report model. This comparison is personal-ledger analysis, not an immutable competition snapshot or audit proof; do not label the stock side as locked or tamper-proof.

## Code-Level Changes In This Baseline

- `/api/quote` now requires a Supabase access token by default.
- `/api/quote` validates and caps the `symbols` parameter.
- `/api/quote` no longer sends wildcard CORS headers.
- Frontend quote calls attach the current Supabase access token.
- `/api/quote?view=market-movers` inherits the same access-token gate and reads `EODHD_API_KEY` only on the server. Its close-only 30/30 universe is the intersection of EODHD `Type=Common Stock` classification and the current official Nasdaq Trader NASDAQ/NYSE/NYSE American directories, with canonical class-symbol matching. It rejects non-stock/stale-date rows, caps EODHD HomeCategory verification at 80 candidates per side per request, and applies a two-minute in-instance failure backoff. It fails closed if either official directory or the provider is unavailable; errors stay sanitized and production must never fall back to an EODHD-only, mock, or estimated ranking.
- `/api/pnl-benchmark` remains bearer-authenticated and server-only for provider access. Stock-detail comparison requests ordinary closes for both the selected stock and QQQ, uses snapshot dates/held shares only for integrity, matches later cash flows against exact ordinary QQQ closes, and fails unavailable on missing or inconsistent inputs without writing any user or snapshot table. The personal snapshots are not competition-grade immutable evidence.
- Multi-account switching stores up to five Supabase browser sessions locally without passwords. Account restoration must use `auth.setSession`, normal logout must stay `scope: 'local'`, and refreshed sessions must replace their older vault entry. Never print, expose through UI, upload, or add analytics around the stored tokens. Treat the vault like Supabase's default persisted browser session: it is JS-readable device-local sensitive data and is not appropriate on a shared or untrusted device.
- Every user-data local cache must include the authenticated `user.id` in its storage key. Database offline caches, cost-basis tool state, wave fold memory, quote diagnostics, and alert memory must not fall back to pre-v311 unscoped keys; a switch must remount `MainApp` by user ID before target-user data renders.
- Browser-direct EODHD WebSocket mode has been removed from the frontend.
- BTC, three-index, and user stock streaming use authenticated server-side WebSocket relays (`/api/btc-realtime`, `/api/indices-realtime`, `/api/stocks-realtime`) and keep `EODHD_API_KEY` server-side. User stock streaming covers watchlist, main ledger positions, wave-record quote rows, and cost-basis tool quote rows.
- Registration uses `/api/register` with server-side invite-code, nickname, and preset-avatar validation. It creates the canonical completed `community_profiles` row before consuming the invite and rolls back the new Auth user on failure; invite-code administration uses `/api/invite-codes` and requires the logged-in admin account.
- Automated P&L and competition snapshots start from the protected `/api/close-snapshot-schedule*` rewrites, defer no-date weekday work until 17:00 ET, and retain separate protected explicit-repair endpoints. The unified scheduler changes only trigger timing and target-date resolution: each runner still returns its own result and writes only its independent snapshot tables. Neither automation mutates `stock_trades` or creates mock returns.
- Quote provider requests now go through timeout-aware provider fetch helpers.
- First automated test baseline covers quote auth, symbol validation, provider routing, timeout behavior, market-mover filtering/error sanitization, and delete scoping.
- `deleteTrade` now scopes deletion by both `id` and `user_id`.
- The V2 wave feature uses a dedicated `swing_waves` row per full buy/full sell, explicit authenticated-only grants, `auth.uid() = user_id` RLS, lifecycle checks, and optimistic concurrency on edits. The production table and RLS are applied and metadata-audited. A two-real-Auth-user SQL/JWT-claim CRUD/RLS smoke passed 14/14 checks and cleanup confirmed no smoke rows; no service-role key was exported. The real standalone page and page-scoped CRUD are deployed as `v10.7.9.297`, runtime commit `b56b7127ab69bd40bee1932c12eab722ebb4064d`.
- The local V2 page must keep wave rows outside the global startup data load. Only unique active symbols may enter the existing authenticated quote universe, together with the minimal REST quote/baseline fields required for safe relay ticks; completed history must not consume relay slots, and formal ledger/watchlist rows stay ahead of tool rows at the 50-symbol cap. It must not weaken `/api/quote`, create a browser-direct EODHD path, or write `trades`, `stock_trades`, `cost_basis_trades`, or P&L snapshot tables.
- The community profile foundation is implemented for `v10.7.9.301` with production SQL applied and anonymous REST exposure blocked. It uses a dedicated `community_profiles` table for public nickname and default-avatar key only; it must not introduce avatar uploads, Supabase Storage, private profile fields, or leaderboard calculations in this rollout.
- The real community competition uses its own authenticated API, opt-in membership table, immutable percentage-only close snapshots, and independent storage/calculation boundary behind the unified automatic scheduler. Its service-only completion marker is published only after a complete batch, and authenticated clients see only sanitized status metadata before deciding whether to fetch the full leaderboard. The authoritative `stock_trade_ledger_revisions` row and database-authored timestamps distinguish a provable current-target-day pure INSERT from every other mutation. Only that narrow case may enter the normal first-snapshot path; otherwise, before any official snapshot/ranking exists, scheduled catch-up may use the service-only forward rebaseline RPC only after strict full-ledger validation, exact positive D1 EOD rows for SPY and every relevant stock, and D1 same-New-York-date pre-close database-time checks. The internal competition deliberately trusts the formal ledger's positive execution price without a provider raw high/low range gate; an old-close substitute is still forbidden. Join, rebaseline, and snapshot guard all lock revision row then member row and use revision CAS to prevent a stale first snapshot. Its internal-test user card may expose only same-date hash-verified holding ticker codes; quantities, cost, amounts, allocation, positions, and trades remain private. It does not change `/api/quote`, `/api/earnings-calendar`, realtime relays, the personal P&L snapshot tables, or any trading-ledger write path.
- Stale duplicate quote implementations were removed; `api/quote.js` is the only quote API entry.
- GitHub Actions CI runs install, test, build, and audit checks.

## Known Follow-Up Work

- Continue validating server-side realtime relays in production after market-data changes.
- Continue splitting the large `src/App.jsx` into feature modules.
- Continue shrinking the quote provider modules and add error-path coverage for EODHD, Yahoo fallback, CNN, and the dedicated EODHD earnings-calendar endpoint.
- Complete metadata-level RLS verification for the remaining user-owned tables; `swing_waves` passed its 13/13 metadata checks.
- Complete a full two-real-user `community_profiles` owner/cross-user isolation smoke when a non-empty service-role or DB admin channel is available; the SQL is applied and anonymous REST boundary is already passing.
- The authenticated two-real-user CRUD/RLS isolation gate for `v10.7.9.297` is complete. The production SQL-editor smoke used two existing `auth.users`, authenticated role/JWT subject claims, and verified 14/14 owner/cross-user/lifecycle/fractional-share checks plus zero residual rows. Repeat this gate after future `swing_waves` policy or schema changes; it did not replace a password-login REST end-to-end test.
