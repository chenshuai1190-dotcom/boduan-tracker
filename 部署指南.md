# 波段跟踪计划 2.0 - 部署指南 

## 你即将做什么

把这个文件夹里的代码上传到 Vercel,自动变成一个网站,然后加到 iPhone 主屏幕变成 App。

---

## 第 1 步:在 Vercel 创建新项目

1. 打开 https://vercel.com,登录你的账号
2. 看到主面板后,点右上角黑色的 **「+ Add New」** 按钮
3. 选择 **「Project」**
4. 你会看到 "Import Git Repository" 页面
5. **不要导入 Git**,在页面下方找到 **「Clone Template」** 或 **「Deploy a template」** 区域
6. **直接关闭这个页面**,我们用更简单的方式

### ✨ 简单方式:用 Vercel CLI 拖拽部署(推荐)

实际上 Vercel 网页版部署必须连 GitHub。但我们有 **Plan B**:

**用 vercel.com/new 的拖拽功能**

1. 打开 https://vercel.com/new
2. 这个页面顶部有个区域可以拖拽 zip 文件
3. **如果看不到拖拽区域**,说明你的 Vercel 账号不支持拖拽,需要走下面的 GitHub 路线

---

## 第 1B 步:如果必须用 GitHub(实际情况)

⚠️ 实话:Vercel 网页版部署**强制要求 GitHub**。但我们可以**不用 git 命令行**,用 GitHub 网页版上传:

### 在 GitHub 网页创建仓库

1. 打开 https://github.com/new
2. **Repository name** 填:`boduan-tracker`
3. **Public** 或 **Private** 都可以(选 Private 更安全)
4. **不要勾选** Add README、.gitignore、license(全部不勾)
5. 点绿色的 **「Create repository」** 按钮

### 网页上传文件

1. 创建后会进入空仓库页面
2. 找到一个链接 **「uploading an existing file」**(在 quick setup 区域),点击
3. 把 **boduan-tracker 文件夹里所有的文件**(不是文件夹本身,是里面的内容)拖拽到上传区域
   - 注意:要包含 `package.json`、`vite.config.js`、`index.html`、`src/`、`api/`、`public/` 等所有内容
4. 等所有文件上传完成(显示绿色对勾)
5. 在下方的 commit message 填:`first commit`
6. 点绿色的 **「Commit changes」** 按钮

### 在 Vercel 导入这个仓库

1. 打开 https://vercel.com/new
2. 找到你刚创建的 `boduan-tracker` 仓库,点旁边的 **「Import」**
3. **关键设置**:
   - **Framework Preset**:选 **Vite**(应该会自动识别)
   - **Build Command**:保持默认 `vite build` 或 `npm run build`
   - **Output Directory**:保持默认 `dist`
4. 在 **Environment Variables**(环境变量)区域添加:
   - `VITE_SUPABASE_URL` = Supabase 项目 URL
   - `VITE_SUPABASE_ANON_KEY` = Supabase anon key
   - `EODHD_API_KEY` = 已轮换的新 EODHD API Key
   - `QUOTE_API_AUTH_REQUIRED` = `true`
   - `QUOTE_ALLOWED_ORIGINS` = `https://boduan-tracker.vercel.app`
   - `VITE_ALLOW_BROWSER_EODHD_WS` = `false`
5. 点最下方的蓝色 **「Deploy」** 按钮
6. 等 1-3 分钟,看到 🎉 烟花动画就成功了
7. 点 **「Visit」** 按钮,看到你的 App!

---

## 第 2 步:加到 iPhone 主屏幕(变成 App)

1. 在 iPhone Safari 打开你的 Vercel 网址(类似 `boduan-tracker-xxx.vercel.app`)
2. 点底部中间的 **分享按钮**(方框 + 向上箭头)
3. 在弹出菜单往下滑,找到 **「添加到主屏幕」**
4. 修改名字(可选),点右上角 **「添加」**
5. 退出 Safari,在主屏幕找到一个新图标(深蓝色 + BD 字样)
6. 点击它,就像打开真正的 App 一样!

---

## 第 3 步:测试

打开你的 App 后:

1. 滚到底部,看到蓝紫渐变的 **「拉取实时行情」** 按钮
2. 点一下,等 2 秒
3. 看到「最近更新:xx:xx:xx」绿色提示,说明数据拉取成功
4. 你会发现:
   - 7 只股票的现价自动更新了
   - VIX 数值也更新了
   - 触发预警的股票会自动变红/黄色
   - 走势图旁边的核心参数也同步更新了

---

## 常见问题

**Q1:点拉取行情后报错 "API key 未配置"**
A:你忘了在 Vercel 设置 `EODHD_API_KEY`,或使用了旧 token。先在 EODHD 后台轮换 token,再回到 Vercel 项目设置 → Environment Variables 更新并重新部署。

**Q2:股价显示 0 或不准确**
A:当前行情经 `/api/quote` 代理 EODHD/Yahoo。先确认已登录,再检查 `EODHD_API_KEY` 是否有效、额度是否充足。

**Q3:VIX 显示不出来**
A:VIX 优先走 EODHD,失败后降级 Yahoo。若两个源都失败,先手动输入,再检查 Vercel Function 日志。

**Q4:实时 WebSocket 为什么不能开?**
A:浏览器直连 WebSocket 会暴露付费 EODHD token,现在默认关闭。要重新启用,必须先做服务端中转。

**Q5:数据会不会丢?**
A:核心数据存在 Supabase。上线前务必执行 `supabase/rls.sql`,确保每个用户只能读写自己的数据。

**Q6:能否多设备同步?**
A:可以,登录同一个 Supabase 用户后会读取同一份云端数据。本地缓存只作为断网兜底。

---

## 以后想改代码?

当前正式开发流程以 `docs/development-process.md` 为准:

1. GitHub 仓库是唯一代码源头。
2. 从当前 `main` 创建分支修改代码。
3. 本地运行 `npm run build`、`npm audit`、`git diff --check`。
4. 每次改动必须同步更新 `docs/development-log.md`。
5. 推送到 GitHub 后,优先通过 PR 合并;权限不足时,必须在用户明确授权后才可快进 `main`。
6. Vercel 从 `main` 自动部署。
7. 部署后验证线上站点和关键接口。

不要在 Vercel 或腾讯云控制台直接在线改源码;那样会让线上代码和 GitHub 脱节,后续很难接手和回滚。

---

## 部署成功后,我能怎么帮你?

如果一切顺利,告诉我「**App 跑起来了**」,我会再帮你:

1. **接入更可靠的行情源**(Polygon、Twelvedata)
2. **加入 Telegram Bot 推送**(下跌触发自动推送到你的 Telegram)
3. **加入云同步**(多设备共用一份数据)
4. **接入长桥 API**(直接看你账户实时盈亏,不用手动输入持仓)

如果有问题,直接截图发给我。
